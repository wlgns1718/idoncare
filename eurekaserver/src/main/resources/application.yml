server:
  port: 18761

spring:
  application:
    name: eurekaserver

eureka:
  client:
    register-with-eureka: false
    fetch-registry: false
    service-url:
      defaultZone: http://k9d103a.p.ssafy.io/:${server.port}/eureka/